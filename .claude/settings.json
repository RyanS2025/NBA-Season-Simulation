{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=\"$CLAUDE_FILE_PATH\"; if [ -n \"$filepath\" ] && echo \"$filepath\" | grep -qE '\\.(ts|tsx)$'; then cd /Users/ryansinha/Documents/code/Portfolio/RSinhaBBALSim/frontend && npx oxlint --fix \"$filepath\" 2>/dev/null; fi; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$CLAUDE_FILE_PATH\" ] && echo \"$CLAUDE_FILE_PATH\" | grep -qE '\\.env|\\.secret|credentials'; then echo 'BLOCK: Refusing to edit sensitive file'; exit 2; fi"
          }
        ]
      }
    ]
  }
}
